{
	"init_body" : "symbol = symbol_val;\n      timeframe = timeframe_val;\n      period = period_val;\n      deviation = deviation_val;\n      bands_shift = bands_shift_val;\n      applied_price = applied_price_val;\n      mode = mode_val;\n      shift = shift_val;"
}
